
  # Create Working Prototype

  This is a code bundle for Create Working Prototype. The original project is available at https://www.figma.com/design/ttWl6F26pQPRntBZU3MLc3/Create-Working-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  